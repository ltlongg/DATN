# Backend API gateway — FastAPI + psycopg (KHÔNG ORM/Alembic, theo backend-plan.md).
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
psycopg[binary]>=3.2
psycopg-pool>=3.2
pydantic>=2.8
pydantic-settings>=2.4
email-validator>=2.0
PyJWT>=2.8
bcrypt>=4.1
# Đăng nhập Google (verify ID token). Extra [requests] kéo theo `requests` cho
# google.auth.transport.requests; cachecontrol để CACHE cert của Google — google-auth
# KHÔNG tự cache, mặc định tải lại cert mỗi lần verify (xem api/auth.py).
google-auth[requests]>=2.38
cachecontrol>=0.14

# Tests
pytest>=8.0
pytest-asyncio>=0.23
